# e2e test artifacts — generated by go test -tags e2e
# workspace temp dirs created by t.TempDir() are in the OS temp dir and
# are auto-cleaned, but any local artifacts go here.
*.log
supervisor.log
